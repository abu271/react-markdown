import React from 'react'

const Preview = (props) => {
    return (
        <div id = "preview">
            <p>
                {props.value}
            </p>
        </div>
    )
}

export default Preview